# Server Configuration
NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# Database Configuration (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://user:password@localhost:5432/nutriguide

# Cache Configuration (Redis)
# Format: redis://HOST:PORT
REDIS_URL=redis://localhost:6379

# Authentication (JWT)
# Generate strong secrets using: openssl rand -base64 32
JWT_SECRET=replace_with_secure_random_string_for_access_token
JWT_REFRESH_SECRET=replace_with_secure_random_string_for_refresh_token
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# Google Gemini AI Service
# Get your key from Google AI Studio
GEMINI_API_KEY=replace_with_your_google_gemini_api_key

# Logging
LOG_LEVEL=debug